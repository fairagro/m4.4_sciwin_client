name: 🦆 CWL Conformance Tests
on:
  workflow_call:

jobs:
  run_tests:
    name: 👀 Run Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - name: Cache Cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Build
        run: cargo build
      - name: Clone CWL
        run: git clone https://github.com/common-workflow-language/cwl-v1.2
      - name: Create Wrapper
        run: |
          echo '#!/bin/bash
          s4n execute local "$@"' > s4n_wrapper.sh
          chmod +x s4n_wrapper.sh
      - name: Run Tests
        continue-on-error: true
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install cwltest
          export PATH=$PATH:$(pwd):$(pwd)/target/debug
          cd cwl-v1.2
          cwltest --tool s4n_wrapper.sh --test conformance_tests.yaml --badgedir badges
      - name: Generate Summary
        run: |
          TABLE="| Subject        | Status | Badge                                      |"
          TABLE="$TABLE\n|----------------|--------|--------------------------------------------|"

          for file in cwl-v1.2/badges/*.json; do
                  SUBJECT=$(jq -r '.subject' $file)
                  STATUS=$(jq -r '.status' $file)25
                  COLOR=$(jq -r '.color' $file)
                  BADGE_URL="https://img.shields.io/badge/$SUBJECT-$STATUS-$COLOR"
                  TABLE="$TABLE\n| $SUBJECT       | $STATUS | ![$SUBJECT]( $BADGE_URL )                  |"
          done

          echo "$TABLE" 
          echo "$TABLE" >> $GITHUB_STEP_SUMMARY
